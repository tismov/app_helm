kind: pipeline
type: helm3
name: default

steps:
- name: deploy_production
  image: pelotech/drone-helm3
  settings:
    helm_command: upgrade
    chart: ./templates
    release: my-project
    api_server:
      from_secret: url
    kubernetes_token:
      from_secret: token