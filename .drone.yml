kind: pipeline
name: default

steps:
- name: deploy_production
  image: pelotech/drone-helm3
  settings:
    helm_command: upgrade
    chart: ./
    release: my-project
    kubernetes_token:
      from_secret: default-token-94v6d
    kube_certificate:
      from_secret: default-token-94v6d-cert
