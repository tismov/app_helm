kind: pipeline
name: default

steps:
- name: deploy_production
  image: pelotech/drone-helm3
  settings:
    helm_command: upgrade
    chart: ./
    release: my-project
    kube_api_server:
      from_secret: url
   # kube_token:
    #  from_secret: token
    kube_certificate: 
      from_secret: cert    
